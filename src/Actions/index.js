export const addStateInfo = (state) => ({
  type: 'ADD_STATE_INFO',
  state
})

export const isLoading = (bool) => ({
  type: 'IS_LOADING', 
  bool
})

export const addStatesList = (states) => ({
  type: 'ADD_STATE_LIST', 
  states
})

